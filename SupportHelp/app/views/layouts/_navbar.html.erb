<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "SupportHelp", root_path, :class => "navbar-brand" %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse navbar-right" id="collapse-1">
      <ul class="nav navbar-nav">
        <li><%= link_to "Home", root_path, class: "Active" %></li>
        <li><%= link_to "Quem Somos", static_pages_about_path %></li>
        <li><%= link_to "Contato", static_pages_contact_path %></li>
        <li>

        <% if current_user.present? %>
          <%= button_to "Sair", destroy_user_session_path(current_user),
               :class => "btn btn-default navbar-btn", :method => :delete  %>
        </div>

        <% else %>
          <div class="btn-group">
            <button class="btn btn-default navbar-btn dropdown-toggle" data-toggle='dropdown' >
              Login ou Cadastrar  <span class="caret"></span>
            </button>
            <ul class="dropdown-menu" role="menu">
              <li><%= link_to "Login", new_user_session_path %></li>
              <li class="divider"></li>
              <li><%= link_to "Cadastre-se", new_user_registration_path %></li>
            </ul>
          </div>
        <% end %>

        </li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
